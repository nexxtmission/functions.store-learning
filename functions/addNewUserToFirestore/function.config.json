{
    "name": "addNewUserToFirestore",
    "description": "Add a user to Firestore when the user signs up.",
    "tags": [
        "firestore",
        "new user",
        "authentication"
    ],
    "trigger": {
        "provider": "auth",
        "event": "user.create"
    },
    "env": [
        {
            "name": "FIRESTORE_COLLECTION_PATH",
            "description": "The collection path that will be used to save the users",
            "required": true
        }
    ]
}